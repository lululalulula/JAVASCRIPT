/*Calcular un promedio es una tarea extremadamente común. Puedes usar este array para probar tu función:
const numbers = [12, 21, 38, 5, 45, 37, 6];
function average(param) {
// insert code

const numbers = [12, 21, 38, 5, 45, 37, 6];
function promedio(param) {
// insert code
}

  //Lanzo la función
  promedio(numbers)*/

/*  Para obtener el promedio, tenemos que resumir los números y luego dividir por el número de valores. Los pasos son los siguientes:

obtener la longitud del array
sumar los valores
obtener el promedio (suma/length)*/
const numbers = [12, 21, 38, 5, 45, 37, 6];
//Para obtener el promedio, tenemos que sumar los números y luego dividir por el número de valores.10 feb 2016
let suma=0;
for (i= 0; i < params.length; i++){
    suma += params[];
  }
